# X Post — final package (max points)

Points math: **3** (main post with video) + **4** (all four Injective techs named in the main post) + **+1 per match** in the comments (4 match screenshots included) = **11+**. Ties break on earliest publish time — post as soon as possible.

## Main post (270/280 chars — attach `video/out/goaloracle-demo.mp4`)

```
AI agents can't sign up for sports APIs. GoalOracle: live World Cup data, free — premium analytics agents buy THEMSELVES. x402 USDC on @injective · CCTP funding · MCP Server · Agent Skills.

github.com/wildanre/goaloracle

@NinjaLabsHQ @NinjaLabsCN #InjectiveGlobalCupHackathon
```

> Requirements covered: overview ✓, real-world WC problem ✓ (agents can't get API keys), demo attached ✓, GitHub link ✓, 3 tags ✓, hashtag ✓, all 4 techs named ✓.

## Replies — post each as a comment on the main post, in this order

Each match reply = +1 point (screenshots use **real World Cup 2026 data** from football-data.org, each bought with a real on-chain x402 settlement — the tx hash is visible in every screenshot).

**Reply 1** — attach `docs/media/x-thread/1-final-spain-argentina.png`

```
Match 1 — THE FINAL 🏆 Spain 1–0 Argentina.

Bought this analysis for $0.01 USDC via x402 — the Poisson model had Spain as 79% favourites. Settlement tx on @injective testnet is right there in the trace. Live World Cup data, paid per call.
```

**Reply 2** — attach `docs/media/x-thread/4-semifinal-france-spain.png`

```
Match 2 — Semifinal: France 0–2 Spain.

Model: Spain 84%, xG 2.34 vs 0.27. Called it again. The agent wallet signed a gasless EIP-3009 authorization; the facilitator settled it on-chain.
```

**Reply 3** — attach `docs/media/x-thread/3-semifinal-england-argentina.png`

```
Match 3 — Semifinal: England 1–2 Argentina.

Form indices, head-to-head, expected goals — deterministic analytics, framed as statistical estimates (never betting advice).
```

**Reply 4** — attach `docs/media/x-thread/2-thirdplace-france-england.png`

```
Match 4 — Third place: France 4–6 England. Ten goals!

Every analysis on this thread was purchased by a wallet, not an API key: HTTP 402 quote → sign → settle on @injective → data.
```

**Reply 5** — engagement/live demo (no image needed, or reuse dashboard screenshot)

```
Try it yourself, no signup:

🌐 goaloracle-kappa.vercel.app — hit "Buy analysis" and watch the x402 trace run
🤖 Agents: MCP server + 3 Agent Skills in the repo — a headless Claude bought a prediction autonomously in our tests

curl https://goaloracle-kappa.vercel.app/premium/match/2001/analysis → HTTP 402
```

## Checklist before posting

- [ ] Attach the video to the MAIN post (not a link — native video gets more reach)
- [ ] Post replies from the same account, as comments on the main post (that's where per-match points count)
- [ ] After posting, paste the post URL into the Typeform: https://xsxo494365r.typeform.com/to/TMaGb1du
